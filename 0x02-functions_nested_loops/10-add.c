#include "main.h"

/**
 * add - sums two integers
 *
 * @a: first number to add
 * @b: second number to add
 * Return: Always 0 (Success)
 *
 */
int add(int a, int b)
{
	int n;

	n = a + b;
	return (n);
}
