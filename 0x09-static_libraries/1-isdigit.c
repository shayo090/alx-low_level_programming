#include "main.h"
#include <stdio.h>

/**
 * _isdigit - checks for a digit
 * @c: input data
 * Return: 1 if c is a digit or 0 otherwise
 */

int _isdigit(int c)
{
	int i = 0, j;
	int n;

	if (n > 0)
	{
		for (; i < n; i++)
		{
			for (j = 0; j < i; j++)
				_putchar(' ');
		_putchar('\\');
		_putchar('\n');
		}
	}
	else
		_putchar('\n');
}
