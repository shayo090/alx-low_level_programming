#include "main.h"

/**
 * 0-isupper - checks for upper case characters
 * @c: upper case value 
 * Return: Always 0.
 */

int _isupper(int c)
{
	int i;

	for(i = 'A'; i <= 'Z'; i++)
		if (c = i)
		{
			return (1);
		}
	return (0);
}
